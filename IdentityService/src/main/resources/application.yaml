spring:
  application:
    name: IdentityService

  # Corrected gRPC configuration block
  grpc:
    server:
      port: 9090

  # 1. DATABASE CONFIGURATION (PostgreSQL)
  datasource:
    url: jdbc:postgresql://localhost:5432/identity_db?currentSchema=identity_db
    username: admin
    password: admin_password
